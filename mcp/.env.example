# Base URL of your Mermaid Live Editor (no trailing slash).
MLE_BASE_URL=https://mermaid.internal.elementdigital.com.au

# Must match MCP_API_TOKEN in the server's server/.env.
MCP_API_TOKEN=mle_pat_replace_with_the_server_token
