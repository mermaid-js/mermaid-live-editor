services:
  mermaid:
    build:
      target: mermaid-dev
    entrypoint:
      - pnpm
      - exec
      - vite
      - dev
      - --host
      - 0.0.0.0
      - --port
      - '8080'
    volumes:
      - ./.env:/app/.env:ro
      - ./src:/app/src
    ports:
      - 24678:24678
